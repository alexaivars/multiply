{
  "name": "multiply-practice",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000 --bind 127.0.0.1 --directory public",
    "test": "node --test tests/*.test.js",
    "test:browser": "playwright test"
  },
  "devDependencies": { "@playwright/test": "1.58.2" }
}
